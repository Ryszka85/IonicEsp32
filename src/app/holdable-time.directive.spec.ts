import { HoldableTimeDirective } from './holdable-time.directive';

describe('HoldableTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new HoldableTimeDirective();
    expect(directive).toBeTruthy();
  });
});
